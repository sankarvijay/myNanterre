language: android
android:
  components:
    - tools
    - build-tools-24.0.1
    - android-24
    - platform-tools
    - extra-android-support # because I'm use support library
    - extra-android-m2repository # because I'm use support library
  licenses:
    - '.+'
language: java

addons:
  sonarcloud:
    organization: "sankarvijay-github"
    token:
      secure: "651beff8ccbbb9026e8df5f6a5fb2e97536f131d"

sudo: false

jdk:
  - oraclejdk8

install: true

before_script:
  - chmod +x gradlew
script:
  - chmod +x gradlew
  - bash <(curl -s https://codecov.io/bash)
  - sonar-scanner
cache:
  directories:
    $HOME/.m2/repository
    $HOME/.sonar/cache
    $HOME/.gradle
    .gradle

after_success:
